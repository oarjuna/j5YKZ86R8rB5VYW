module.exports = {
	run: function(Hive) {

    // move to spawn for recyling when close to death
    // creep flow
      // receive orders
      // locate dest_id
      // move towards dest_id
      // if in range of dest_id
        // harvest/transfer from/transfer to/upgrade/repair to dest_id until energy is empty or full or build/repair is complete.
          // update job status as complete
          // set state to idle

  }
};
